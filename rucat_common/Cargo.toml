[package]
name = "rucat_common"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
surrealdb = {"version" = "1"}
axum = {"version" = "0"}
anyhow = {"version" = "1"}
tonic = "0"
prost = "0"
serde_json = "1"

[build-dependencies]
tonic-build = "0"